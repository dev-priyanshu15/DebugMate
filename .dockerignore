node_modules
.next
.git
.gitignore
# Exclude all local env files — secrets must never be baked into image
# layers.  NEXT_PUBLIC_* vars reach the build via docker-compose build
# args (they are public values, not secrets).  Server-side secrets are
# injected at runtime via docker-compose env_file.
.env*.local
.env.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
README.md
.dockerignore
Dockerfile
docker-compose*.yml
*.md
.vscode
.idea
coverage
